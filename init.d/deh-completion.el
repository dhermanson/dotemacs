(setq completion-styles
      '(
        ;; flex
        helm-flex
        ))

;; (setq completion-styles
;;       '(
;;         ;; flex
;;         helm-flex
;;         basic
;;         partial-completion
;;         emacs22))


;; upgrade to emacs 27 to enable flex completion style


(provide 'deh-completion)
