# -*- mode: snippet -*-
# name: constructor
# key: c
# --
constructor(${1:props}) {
                        super($1);
                        this.state = {
                                   $0
                        };
}